
<%= form_for(task) do |f| %>
<% if task.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

    <ul>
    <% task.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<div class="form-group">
  <%= f.label :titre %>
  <%= f.text_field :titre, class: 'form-control' %>
</div>
  <div class="form-group">
    <%= f.label :contenu %>
    <%= f.text_area :contenu, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :auteur %>
    <%= f.text_field :auteur, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :status %>

    <%= f.select(:status, [['A faire',1], ['En cours',2], ['Terminé',3]]) %>
  </div>
  <button type="submit" class="btn btn-primary"><%= f.submit "Valider" %></button>

<% end %>
</div>
